# AgentBridge scripts

## Smoke tests

```powershell
npm run test:codex -- "C:\path\to\project" "your prompt"
npm run test:cursor -- "C:\path\to\project" "your prompt"
```

These call the production adapters in `server/agents/`.

## Prototypes

See `prototypes/` for archived PoC bridge scripts (pre-migration reference only).
